# 解析历史记录功能需求文档

## 1. 文档信息

- 功能名称：解析历史记录
- 适用项目：云评 / 网易云音乐评论查看器
- 目标平台：Android，兼容桌面端预览
- 当前支持的解析类别：专辑、歌手
- 文档版本：V1.0
- 编写日期：2026-06-09

## 2. 背景与目标

当前应用在解析网易云音乐专辑或歌手链接后，仅在当前页面展示解析结果。用户清空页面、发起新的解析或退出应用后，之前的解析结果无法再次查看。

本功能需要将成功完成的解析结果保存到本地，并提供独立的“解析历史”页面，使用户能够：

1. 查看过去的解析记录。
2. 查看某条历史记录的完整解析结果。
3. 区分记录的解析类别，例如“专辑”或“歌手”。
4. 删除指定历史记录。
5. 多选并批量删除历史记录。

## 3. 功能范围

### 3.1 本期包含

- 成功解析后自动保存历史记录。
- 解析历史列表页面。
- 历史记录详情页面。
- 解析类别展示。
- 单条删除。
- 多选删除。
- 全选与取消全选。
- 删除前二次确认。
- 历史记录本地持久化。
- 空状态、加载失败和数据异常提示。

### 3.2 本期不包含

- 云端同步和跨设备同步。
- 用户账号体系。
- 历史记录导入、导出和分享。
- 对历史记录进行编辑或重命名。
- 自动清理过期记录。
- 保存解析失败或未完成的任务。

## 4. 用户场景

### 4.1 查看历史记录

用户完成若干次专辑或歌手解析后，进入“解析历史”页面，可以按照解析时间从新到旧查看所有历史记录。

### 4.2 查看历史解析结果

用户点击某条历史记录，进入历史详情页面，看到该次解析时保存的摘要信息和完整结果列表，无需重新请求网络。

### 4.3 删除单条记录

用户在历史列表中对指定记录执行删除操作，确认后该记录从本地永久删除。

### 4.4 批量删除记录

用户进入多选模式，勾选一条或多条历史记录，点击“删除”，确认后批量删除已选记录。

## 5. 页面与交互需求

### 5.1 主查询页面调整

当前主查询页面增加进入“解析历史”页面的入口。

要求：

- 入口位置清晰，建议放在页面顶部标题区域或主操作按钮区域。
- 点击入口后进入解析历史列表页面。
- 返回主查询页面时，当前输入和当前解析结果应尽量保留。
- 解析成功且结果已完整展示后，自动保存一条历史记录。
- 保存成功不应打断当前结果展示。
- 相同链接被多次成功解析时，默认分别保存为多条记录，以保留不同时间点的数据。

### 5.2 解析历史列表页面

历史列表默认按解析完成时间倒序排列，最新记录显示在最上方。

每条历史记录至少展示：

- 解析标题：优先展示专辑名；歌手解析可展示解析摘要或“歌手 #ID”。
- 解析类别：以“专辑”或“歌手”标签展示。
- 解析时间：精确到分钟。
- 结果摘要：展示本次解析的 `summary`。
- 结果条数：展示保存的结果行数量。

列表操作：

- 点击记录：进入历史详情页面。
- 单条删除：通过删除按钮或长按菜单触发。
- 进入多选模式：点击“多选”按钮或长按记录触发。
- 多选模式下，每条记录显示选中状态。
- 多选模式顶部或底部显示“已选 N 条”。
- 支持“全选”“取消全选”“删除”“退出多选”。
- 未选择记录时，批量删除按钮应禁用。

空状态：

- 没有历史记录时，显示“暂无解析历史”。
- 提示用户返回首页完成一次解析。

### 5.3 历史详情页面

历史详情页面用于展示某次解析时保存的数据，不重新发起网络请求。

页面至少展示：

- 解析类别。
- 原始输入内容或原始链接。
- 解析对象 ID。
- 解析完成时间。
- 结果摘要。
- 完整解析结果列表。

结果列表的展示样式应尽量复用当前主查询页面的结果列表：

- 专辑或歌曲名称。
- 评论数。
- 专辑标题行。
- 歌曲结果行。

详情页操作：

- 返回历史列表。
- 删除当前历史记录。
- 删除前必须二次确认。
- 删除成功后返回历史列表，并刷新列表内容。

### 5.4 删除确认

所有删除操作均需要二次确认。

单条删除提示示例：

> 确定删除这条解析记录吗？删除后无法恢复。

批量删除提示示例：

> 确定删除已选择的 N 条解析记录吗？删除后无法恢复。

要求：

- 用户确认后执行删除。
- 用户取消后不修改任何数据。
- 删除失败时提示原因，并保留未成功删除的记录。

## 6. 历史记录保存规则

### 6.1 保存时机

满足以下条件时保存历史记录：

- 专辑或歌手链接识别成功。
- 网络解析任务正常完成。
- 已获得结果列表和摘要。

以下情况不保存：

- 链接无法识别。
- 网络请求或解析过程抛出异常。
- 应用在解析过程中被关闭。
- 返回结果尚未完整生成。

### 6.2 保存内容

每条历史记录建议包含以下字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `history_id` | string | 是 | 历史记录唯一 ID，建议使用 UUID |
| `category` | string | 是 | 解析类别：`album` 或 `artist` |
| `entity_id` | string | 是 | 专辑 ID 或歌手 ID |
| `source_text` | string | 是 | 用户提交的原始输入内容 |
| `title` | string | 是 | 历史列表展示标题 |
| `summary` | string | 是 | 本次解析摘要 |
| `rows` | array | 是 | 本次解析得到的完整结果列表 |
| `row_count` | integer | 是 | 结果行数量 |
| `created_at` | string | 是 | 解析完成时间，使用 ISO 8601 格式 |
| `schema_version` | integer | 是 | 历史数据结构版本，初始值为 `1` |

`rows` 中每项沿用当前解析结果结构：

| 字段 | 类型 | 说明 |
|---|---|---|
| `album` | string | 专辑名称 |
| `song` | string | 歌曲名称或专辑描述 |
| `comments` | string | 评论数或获取失败提示 |
| `is_header` | boolean | 是否为专辑标题行 |

### 6.3 标题生成规则

- 专辑解析：使用第一条标题行的 `album` 字段作为标题。
- 歌手解析：当前接口结果没有返回歌手名称，本期使用“歌手 #实体ID”作为标题。
- 如果无法生成标题，使用“未命名解析记录”。

## 7. 本地存储需求

### 7.1 存储方式

本期建议使用 JSON 文件进行本地持久化，避免新增第三方依赖，符合当前项目的小型应用结构。

要求：

- 存储文件放在 Kivy 提供的应用私有数据目录中，不写入项目源码目录。
- Android 端卸载应用后，历史记录允许随应用数据一并删除。
- 应用启动时读取历史记录。
- 新增或删除记录后立即持久化。
- 写入时采用临时文件加替换的方式，降低文件损坏风险。
- 文件不存在时按空历史处理。
- 文件内容损坏时不应导致应用崩溃，应提示历史数据读取失败并允许继续使用查询功能。

建议存储文件名：

```text
parse_history.json
```

### 7.2 数据排序

- 默认按 `created_at` 倒序展示。
- 保存顺序不应成为唯一排序依据。

### 7.3 数据兼容

- 使用 `schema_version` 标记数据结构版本。
- 遇到缺少非关键字段的旧记录时使用默认值展示。
- 遇到无法识别的记录时跳过该记录，并保留其他有效记录。

## 8. 业务规则

1. 历史记录只保存解析完成时的数据快照。
2. 打开历史详情时不自动刷新评论数。
3. 删除操作只影响历史数据，不影响当前页面已展示的解析结果。
4. 清空主查询页面不删除历史记录。
5. 多选状态仅在历史列表页面内有效，离开页面后自动退出多选状态。
6. 批量删除按历史记录唯一 ID 执行，避免因列表顺序变化删除错误记录。
7. 未知解析类别应显示为“未知”，但记录仍允许查看和删除。

## 9. 异常与提示

| 场景 | 期望行为 |
|---|---|
| 历史文件不存在 | 展示空历史页面 |
| 历史文件无法读取 | 提示读取失败，查询功能仍可使用 |
| 历史文件部分记录异常 | 跳过异常记录，展示其他有效记录 |
| 保存历史失败 | 当前解析结果正常展示，同时提示“历史记录保存失败” |
| 单条删除失败 | 提示删除失败，列表保持原状 |
| 批量删除部分失败 | 明确提示失败数量，并刷新剩余记录 |
| 详情对应记录已不存在 | 提示记录不存在并返回历史列表 |

## 10. 非功能需求

### 10.1 性能

- 历史列表页面打开后应在 1 秒内完成本地数据展示。
- 保存和删除历史记录时不应明显阻塞界面。
- 列表包含 500 条历史记录时，仍应能够正常浏览和删除。

### 10.2 稳定性

- 历史数据读取、保存和删除异常不能导致应用崩溃。
- 保存过程中应用异常退出时，应尽量保留原有历史文件。

### 10.3 可用性

- 类别标签、删除操作和多选状态应清晰可辨。
- 删除按钮应与普通查看操作有明显视觉区别。
- 危险操作必须确认，避免误删。

### 10.4 隐私

- 历史记录仅保存在应用本地私有目录。
- 不上传用户历史记录或原始输入内容。

## 11. 建议代码结构

为减少对当前解析逻辑的影响，建议按职责拆分：

```text
main.py                 # 页面导航、主查询页面、历史列表与详情页面
history_store.py        # 历史记录读取、写入、新增、删除、批量删除
netease_api.py          # 保持现有网络解析职责
```

建议新增历史存储接口：

```python
load_history() -> list
save_record(record: dict) -> None
delete_record(history_id: str) -> bool
delete_records(history_ids: list[str]) -> int
get_record(history_id: str) -> dict | None
```

页面导航建议使用 Kivy `ScreenManager`，至少包含：

- `QueryScreen`：主查询页面。
- `HistoryScreen`：解析历史列表页面。
- `HistoryDetailScreen`：历史记录详情页面。

## 12. 验收标准

### 12.1 保存与查看

- [ ] 完成一次专辑解析后，历史列表新增一条“专辑”类别记录。
- [ ] 完成一次歌手解析后，历史列表新增一条“歌手”类别记录。
- [ ] 解析失败时，历史列表不新增记录。
- [ ] 退出并重新打开应用后，历史记录仍然存在。
- [ ] 点击历史记录后，可以查看该次解析的摘要和完整结果。
- [ ] 查看历史详情时不会发起新的网络解析请求。

### 12.2 单条删除

- [ ] 用户可以从历史列表删除指定记录。
- [ ] 用户可以从历史详情页删除当前记录。
- [ ] 删除前显示确认提示。
- [ ] 取消删除后记录保持不变。
- [ ] 确认删除后记录立即从列表和本地存储中移除。

### 12.3 多选删除

- [ ] 用户可以进入和退出多选模式。
- [ ] 用户可以选择和取消选择任意记录。
- [ ] 页面正确显示已选择记录数量。
- [ ] 用户可以全选和取消全选。
- [ ] 未选择记录时批量删除按钮不可用。
- [ ] 确认批量删除后，仅删除已选择记录。
- [ ] 取消批量删除后所有记录保持不变。

### 12.4 异常处理

- [ ] 历史文件不存在或为空时，应用正常显示空状态。
- [ ] 历史数据损坏时，应用不会崩溃。
- [ ] 历史保存失败时，不影响当前解析结果展示。

## 13. 实施优先级

### P0：必须完成

- 历史记录本地保存。
- 历史列表页面。
- 历史详情页面。
- 类别展示。
- 单条删除。
- 多选和批量删除。
- 删除确认。
- 基础异常处理。

### P1：建议完成

- 全选和取消全选。
- 历史数据结构版本兼容。
- 500 条历史记录的性能验证。

### P2：后续可扩展

- 按类别筛选。
- 按标题搜索。
- 手动重新解析历史记录。
- 导出历史解析结果。
- 自动清理或设置最大保存数量。
